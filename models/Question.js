class Question {
    constructor(text, options, correctAnswer) {
        this.text = text; // Textul întrebării
        this.options = options; // Variantele de răspuns
        this.correctAnswer = correctAnswer; // Răspunsul corect
    }
}

export default Question;
